---
/**
 * index.astro — Main portfolio page.
 * Assembles all section components in order.
 * Achievements and Testimonials are only rendered when their arrays are non-empty.
 */
import BaseLayout from '@layouts/BaseLayout.astro';
import Hero from '@components/Hero.astro';
import About from '@components/About.astro';
import Skills from '@components/Skills.astro';
import Experience from '@components/Experience.astro';
import Projects from '@components/Projects.astro';
import Certifications from '@components/Certifications.astro';
import Achievements from '@components/Achievements.astro';
import GitHubStats from '@components/GitHubStats.astro';
import Testimonials from '@components/Testimonials.astro';
import Contact from '@components/Contact.astro';
import { portfolio } from '@config/portfolio';

const showAchievements = portfolio.achievements.length > 0;
const showTestimonials = portfolio.testimonials.length > 0;
---

<BaseLayout>
  <Hero />

  <div class="section-divider"></div>
  <About />

  <div class="section-divider"></div>
  <Skills />

  <div class="section-divider"></div>
  <Experience />

  <div class="section-divider"></div>
  <Projects />

  <div class="section-divider"></div>
  <Certifications />

  <div class="section-divider"></div>
  <GitHubStats />

  {showAchievements && (
    <>
      <div class="section-divider"></div>
      <Achievements />
    </>
  )}

  {showTestimonials && (
    <>
      <div class="section-divider"></div>
      <Testimonials />
    </>
  )}

  <div class="section-divider"></div>
  <Contact />
</BaseLayout>
